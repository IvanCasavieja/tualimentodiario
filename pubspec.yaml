name: tu_alimento_diario
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_localizations:
    sdk: flutter

  # Firebase
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4

  # Estado / utils
  flutter_riverpod: ^2.5.1
  intl: ^0.20.2
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Descomenta cuando agregues los PNG reales:
  # assets:
  #   - assets/avatars/avatar_01.png
  #   - assets/avatars/avatar_02.png
  #   - assets/avatars/avatar_03.png
